<Window x:Class="OhjelmistokehitysProjekti.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OhjelmistokehitysProjekti.Views"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800"
        Background="Gray">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="300"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="60"></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Content="Username:" ></Label>
        <Label Grid.Column="0" Grid.Row="1" Content="Password:" ></Label>
        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Username, Mode=OneWayToSource}" ></TextBox>
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Password, Mode=OneWayToSource}"></TextBox>
        <CheckBox Grid.Row="2" Content="Create new User" IsChecked="{Binding LoginOrSignup, Mode=TwoWay}"></CheckBox>
        <Button Grid.Row="2" Grid.Column="1" Content="{Binding LoginMode}" Command="{Binding LoginCommand}"></Button>
        <Button Grid.Row="3" Grid.Column="1" Content="Play as a guest" Command="{Binding PlayAsGuestCommand}"></Button>
        <Button Name="ExitBtn" Grid.Row="4" Grid.Column="1" Content="Quit" Command="{Binding CloseWindowCommand}"></Button>
    </Grid>
</Window>
